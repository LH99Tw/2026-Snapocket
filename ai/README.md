# AI Workspace

현재 `ai` 폴더는 아래 4개 영역으로 구성됩니다.

- `model/` : (선택) 실험/아카이브 모델 파일 보관
- `service/` : 프론트+백엔드 통합 서비스 코드
- `data/` : DB 및 테스트 데이터
- `markdown/` : 설계/작업 문서

현재 서비스 런타임 OCR은 `Ollama` 기반입니다.

세부 의도와 변경 내역은 `markdown/refactor_intent.md`를 참고하세요.
